@model COnt.Models.Employee
    @using COnt.Models
<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    @{
        bool _istrue = false;
    }
    <div>
        @if (_istrue)
        {
            <label>sorry this is true</label>
        }
        else
        {
            <b>hihi this is false</b>
        }
    </div>
    <div>
        @for (int i = 0; i < 6; i++)
        {
            <label>this is index number: @i</label>
            <br />
        }
    </div>
    <div>
        @(_istrue ? "this true" : "this is flase")
    </div>
    <div>
        @{
            var list = new List<int> { 1, 2, 3, 4, 5 };
        }
        @foreach (var lis in list)
        {
            <b>I'm the Boss = @lis</b><br />

        }
        @{
            Employee employee = new Employee() { id = 1, Name = "Doflamingo" };
        }
    </div>
    <div>
        id= @employee.id  <br />
        namme=@employee.Name

    </div>

    <div>
        @helper myList(string[] str)
        {
            if (str.Length == 0)
            {
                <label>Sorry Empty List</label>
            }
            else
            {
                <ul>
                    @foreach (var item in str)
                    {
                        <b>@item</b>
                        <br />
                    }
                </ul>
            }
        }
    </div>
    <b> this is Condition nd index menthod</b>
    <div>
        <label>
            Ist List
        </label>
        <br />
        @myList(new string[] { "vikas", "fuck", "you" })
    </div>
    <div>
        <label>
            2nd List
        </label>
        <br />
        @myList(new string[] { })
    </div>
</body>
</html>
